# Independent source/native semantic comparison

Reviewed native candidate `work/beamng/decommit-edge-fix/native/Madeira` SHA256 `6e09addbcea3478bb2fb4c6989703351849f0aff160fe3d4c49eb8e3ed5697a8` and assembly SHA256 `a2233aaef93e67709641193d7477de81975831aa5f77f5fd6b6b4a7ddab386c2`, against source fragment SHA256 `e584af35f99fc879679b7385ab813fefb182b7ca1426c22b3d331d289b9d70c6`.

Read-through found no substantive mismatch for valid mapped Wine views and actual bounded pool-global ranges. Native two40-byte records are initialized before preparation, so cleanup can safely scan both records even if preparation fails on the first. The C source increments its count before each prepare call and scans only those records; these are equivalent because unprepared native records have changed=0. Both prepare both partial edges before interior mmap; both support two partial host pages with zero complete interior pages, avoid double-clearing a single-page interval, retain alias-at-base/live-ledger refusal, and restore changed edges in reverse order.

Checked native logic includes complete Mach range/count coverage, object-port release, original/max snapshots, !EXEC and sufficient maximum R/W, exact current/max query after opening and restoring, whole-page alias-table and pool intersection guards, checked MAP_FAILED, edge-only BZERO and real byte-readback, and failure propagation. The alias-refusal variadic arguments use Darwin's stack convention and the original format string. No additional stack frame or guest x18 use is introduced; saves remain within the original0x190-byte frame and original epilogue location. The native harness still must validate actual instruction execution and unwind/bounds details.

The source retains the kernel_writewatch_register_range call; pinned Darwin's implementation is an empty function at original source lines5014–5016, so native omission is equivalent. Native COMMITTED-bit updates inline the existing two-level pages_vprot table rather than calling the C helper.

Independently disassembled the new caller instruction:0x100134a70 branches to0x100134a98, which copies x0 into x24 and checks w0 before following the existing0x100134978 lock-release/error path. Successful calls retain the original output writes. This is necessary because the old function always returned success and the old caller discarded its return value.

Native pool overlap uses addition for pool end; source uses subtraction to avoid arithmetic wrap. Native page-bookkeeping rounding similarly assumes a valid mapped-view end. These expressions agree for the actual supported pool and view addresses; arbitrary near-UINT64_MAX fictitious mappings are not covered by that equivalence. This review did not execute Darwin Mach calls or independently rerun the ARM64 harness and does not establish device success. Host source fixture PASS78 is separate evidence with its stated kernel-model limits.
